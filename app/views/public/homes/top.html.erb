<div class="container">
  <div class="row">
    <div class="col-lg-4">
      <%= render'genres' %>
    </div>
    <div class="col-lg-8">
      <div style="text-align: center;">
        <p class="border border-dark">
          ようこそ、ながのCAKEへ！<br>
          このサイトは、ケーキ販売のECサイトになります。<br>
          会員でない方も商品の閲覧はできますが、<br>
          購入には会員登録が必要になります。
        </p>
      </div>
      <h2>新着商品</h2>
      <% @items.each do |item| %>
      <table>
        <tr>
          <%= link_to item_path(item.id) do %>
            <%= image_tag item.get_image, size: "100x100" %><br>
          <% end %>
          <span><%= item.name %></span>
          <span>¥<%= item.price_excluding_tax %></span>
        </tr>
      </table>
      <% end %>
      <%= link_to "全ての商品を見る＞", items_path %>
    </div>
  </div>
</div>