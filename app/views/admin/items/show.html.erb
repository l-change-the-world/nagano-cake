<div class="mt-5">
  <div class="container">
    <div class="row">

      <div class="col-4">
        <h3 class="bg-light text-center col-8">商品詳細</h3>
      </div>
      <div class="col-8"></div>

      <div class ="col-5 text-center mt-5">
        <%= image_tag @item.get_image, size: "250x200" %>
      </div>
      <div class ="col-7 mt-5">
        <table class="table table-borderless height="50"">
          <tr>
            <th>商品名</th>
            <td><%= @item.name %></td>
          </tr>
          <tr>
            <th>商品説明</th>
            <td><%= @item.description %></td>
          </tr>
          <tr>
            <th>ジャンル</th>
            <td><%= @item.genre.name %></td>
          </tr>
          <tr>
            <th>税込価格（税抜価格）</th><!--↓モデルで定義した税込価格add_tax_sales_priceメソッドを呼び出して税込価格表示-->
            <td><%= @item.add_tax_sales_price.to_s %> ( <%= @item.price_excluding_tax %> ) 円</td>
          </tr>
          <tr>
            <th>販売ステータス</th>
            <td>
              <p class="<%= @item.is_for_sale ? 'text-success font-weight-bold' : 'text-secondary font-weight-bold' %> ">
                <%= @item.is_for_sale ? '販売中' : '売切れ' %>
              </p>
            </td>
          </tr>
        </table>
      </div>

      <div class="text-center col">
        <%= link_to "編集する", edit_admin_item_path(@item.id), class: "btn btn-success" %>
      </div>
    </div>
  </div>
</div>